# ge
GE is a single-process C++ graph execution engine for media, inference, and custom data flows. Its control plane validates  JSON graph specs, negotiates edge contracts, and atomically publishes immutable topologies; the data plane streams packets with backpressure. Supports live mutations, hot parameter updates, C-ABI plugins.
